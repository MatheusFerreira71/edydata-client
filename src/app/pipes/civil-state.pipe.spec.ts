import { CivilStatePipe } from './civil-state.pipe';

describe('CivilStatePipe', () => {
  it('create an instance', () => {
    const pipe = new CivilStatePipe();
    expect(pipe).toBeTruthy();
  });
});
